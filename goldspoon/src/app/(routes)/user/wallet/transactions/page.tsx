import { Button } from "@/components/ui/button"

export default function Home() {
  return (
    <div>
      <Button variant={"destructive"}>Click me -> add admin chagrges as well at the end</Button>
    </div>
  )
}
